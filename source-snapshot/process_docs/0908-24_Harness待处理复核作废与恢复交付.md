# H08：待处理复核作废与恢复交付

- 时间：2026-09-08 01:46:27 GMT+8。
- 触发：继续H06真实验收所暴露的交付阻塞，核心复核状态与恢复语义变更、验证里程碑。
- 工作区：5977 / codex/vercel-delivery-contract-20260907，基线397e8dd（含已整合H07）；本阶段不改浏览器执行器、页面观察或WP schema。

## 问题与行为

H06模型选错复核片段后留下pending包；当前版本只要存在pending便无法发布，却没有显式放弃该包的入口。新增discardDeliveryReview(receiptId,reason)，只将当前任务未提交的pending记录置为discarded，理由trim后10–600字符。保留id、文件版本、evidenceRef和原归档；原因不可改写，仅接受相同原因重试。已提交复核（即便findings为空）及已有发现不可作废，已作废记录不可重新提交为已审查。取消后不改状态。

checkDeliveries的索引披露作废状态和原因，sourceReviews分开统计pendingReceipts/discardedReceipts；作废不计已审查字符或submitted。全部作废仍缺复核，必须存在当前版本的已提交复核且无pending，所有发现仍阻止交付。48条总取证额度不释放，不能靠反复作废无限取包。用户要求、发布文件版本与来源证据不因作废而变化。

## 验证

基线新增用例66项中61通过5失败（缺少作废入口）；首次两项it.each参数误展开导致测试自身校验错误，改成对象参数后再运行上述基线，不把测试错误归于产品。新增作废/恢复/不可撤销/伪造/取消/额度/原生循环用例后：候选typecheck、lint、4文件122项通过（2026-09-08 01:45:12，4.04秒），生产build通过。

原生Vercel测试先保留一个pending和一个已提交包，任务结束失败暂停；保存/恢复后用新工具作废pending，再checkDeliveries与publishFile，4次脚本模型请求内完成；原成果v1未重写、原已提交记录逐字保持、归档引用不变。模型为MockLanguageModelV4，持久恢复为structuredClone状态重建，不声称新增真实模型或Chrome重启业务通过。H06原JSON坏检查及文章置信度丢失仍未修复。

## 兼容、文档与集成

复用DeliveryState与既有持久化，没有数据库版本迁移；状态枚举新增discarded及可选discardReason，旧pending/model-reviewed记录继续可读。含新状态的任务若由旧版代码处理可能不识别其语义，回退时需同时考虑持久状态，不能把无DB迁移等同可任意降级。

作废理由仍是模型声明，代码不验证其语义正当性；保留审计和已提交发现，不把此机制当全文忠实证明。现有局部复核的限制继续披露。模块说明新增接口/状态/计数，主架构相应更新持久状态语义入口，项目状态与合并清单新增H08。Pi任务已收到共享修改边界通知，未在Pi分支实施，不声称对方已经整合。

源提交冻结后核对主区对应文件是否仍干净，再按确切源码合入codex/agent-workspace-sandbox，重新验证主区组合；执行器冻结快照保持原样。

## 主区整合验证

2026-09-08 01:48:46 GMT+8：源3a4e7dc已正常合入codex/agent-workspace-sandbox/5e40a00a91d8f525c623070fa3261b1b72157294。原主区f7c496f已含前端708534a及其组合提交，首次ff-only按预期拒绝，随后普通merge保留双方，不覆盖主区在途修改。主区typecheck/lint及6文件139项通过（01:47:36，5.39秒）；包括交付组件/原生恢复、上下文、运行器、工具分组与WordPress工作流。主区WP等仍有新增未提交用例，故与候选4文件122项及H07旧时点分开记录，不能算同一次干净版本全量测试。

H07工具按需启用与H08待处理复核作废均完成技术集成。下一优先级为H06错误JSON检查登记/冻结的修复设计与反例；不允许为了让任务通过任意删除已经冻结的用户约束。来源概率限定遗漏仍需独立内容核验。本轮零新增付费请求，未重载用户Comet，浏览器执行器重构/冻结副本由其任务继续。
