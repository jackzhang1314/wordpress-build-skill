# H09：JSON对象检查与冻结前格式自测

- 时间：2026-09-08 01:59:06 GMT+8。
- 触发：用户继续授权Harness交付修复；关键策略决定与首轮回归。
- 工作区：5977/codex/vercel-delivery-contract-20260907；产品基线378367f，本阶段尚未提交。

H06将rows/total对象字段当成jsonArray的嵌套路径并冻结。现有checks没有对象字段类型检查；单靠发布失败无法判定是稿件错还是模型解释错，所以不开放任意解锁/降低旧约束的后门。当前新增jsonObject（逐级keys、直接properties与基础类型、exact额外字段开关），并对新JSON登记要求exampleJson最小格式样例，在冻结前验证kind/路径/字段类型是否自洽。样例不检查数组数量、不作为实际金额/业务正确答案；真正发布仍检查完整原文件与原数量。

复用依赖Zod官方safeParse、number/int/record/array类型校验与原生JSON.parse，不增加通用JSON Schema引擎或第二个模型判官；AI SDK工具schema/执行循环保持。产品特定的薄适配仅负责字段清单、路径、数量及登记原子性。没有依靠中文正则猜测用户期望的JSON结构。

新增/更正的登记缺样例或结构不符整单不生效，模型可修正首次解释；已冻结文件、用户原文、有效条件不允许自行改写。旧无样例的冻结数组契约仍可读取、原样重试及校验，历史坏契约不自动迁移为对象契约，需用户实际更正后重新登记。用户只说继续仍保留旧约束。样例能发现解释内部矛盾，不能证明用户语义映射正确；模型若连样例也构造错，此机制不能保证发现。

首轮测试：旧实现79项中11失败68通过，包含H06错误登记未拒绝、对象检查缺失；修复后typecheck/lint与2文件91项通过（01:57:54，1.78秒）。旧原生数组登记fixture补exampleJson=[]以符合新登记要求；实际数量断言仍不变。下一步补齐反例（额外字段、重复字段、嵌套/混合类型、原子性）、原生拒绝→纠正→发布→恢复，以及主区集成验证；未运行新付费任务，不改H06冻结输入/成果。

## 候选实现与最终验证

最终新增jsonObject的keys/properties/exact与JSON检查可选持久字段exampleJson。新/更正登记必须给样例；仅已冻结且内容完全相同的历史登记可继续缺省。properties最多40字段，样例最多4000字符，原整单16000字符上限保留。字段类型为integer（安全整数）、number（有限数）、string、boolean、null、array、object；array/object只检查容器类型，深层约束需明确路径和另加检查。exact只作用目标对象直接字段，用户未要求“只包含”时不应设置true。

原生AgentRunner→ToolLoopAgent回归使用H06原请求及已保存结果v1：原错误登记缺样例先拒绝，补对象样例但仍用rows.total路径再次拒绝，之后jsonObject首次登记成功并直接发布原v1；5次脚本模型请求、2个预期工具错误。恢复后契约及检查保留。另验证旧错误契约保持阻塞，只有用户明确更正后新登记才可发布同一成果版本。未运行新真实模型，未修改旧H06结果。

审查发现Zod record解析会去掉__proto__键，因此exact/字段存在性针对原JSON对象的own keys核对，字段类型仍复用Zod；额外键不能借解析转换消失。新增此反例及嵌套、混合类型、禁止隐式转换、重复字段、样例错误整单原子拒绝、冻结后样例不可变、负数/非整数/超安全整数/非有限数检查。没有新第三方依赖。

最终候选typecheck/lint、5文件143项（2026-09-08 02:02:31，4.40秒）及生产build通过。主区组合源码/测试及整合结果待提交后登记。候选模块说明同步，主架构/项目状态/合并清单按候选边界更新。新jsonObject契约不能假定旧代码可处理；旧jsonArray正常读取，不自动降级或迁移。

## 主区整合与收尾

记录时间：2026-09-08 02:07:39 GMT+8；触发：阶段整合与组合验证完成。源提交a97b2ae0d8825f89a2e1154ab8c893263916466a，工作区5977、分支codex/vercel-delivery-contract-20260907；目标主区codex/agent-workspace-sandbox由378367f快进至同一提交。仅整合delivery.ts、两个交付测试、模块说明和本过程记录五个文件；其他任务在途修改未纳入提交。

主区2026-09-08 02:05:12运行typecheck、lint及6文件159项相关测试全部通过（5.58秒），覆盖交付内容/原生循环、Harness运行与上下文、工具选择及主区WordPress工作流。此结果绑定主区当时的工作区组合，并非整个产品干净提交的全量验收。候选143项与生产build属于源a97b2ae对应代码；本轮没有重建主区dist、重载用户Comet或运行新真实模型/浏览器业务任务。

主架构、项目状态与唯一合并清单同步H09；它们原为主区共享未跟踪文档，按职责只改本任务内容，不随本次整包提交。无数据库迁移；旧jsonArray可继续校验，新jsonObject不保证被旧代码识别。历史坏契约仍需实际用户更正，H06真实业务失败记录保留。下一步继续处理来源限定在交付内容中丢失的问题，先审查现有复核能力；浏览器执行器和前端消息协议继续由对应任务负责。
