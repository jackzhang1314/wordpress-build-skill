# Vercel Harness 与浏览器执行器职责边界

- 时间：2026-09-08 01:25:50 GMT+8。
- 触发：用户要求查看「浏览器执行器优化」，避免本任务重复实施。
- 工作区：5977 / codex/vercel-delivery-contract-20260907，当前953ba65；本轮仅范围和文档更新，未改产品代码、未运行模型。
- 证据：任务列表确认浏览器执行器任务01a06ebc-90b6-76d2-84cc-c41817fd07c1正在主区运行。read_thread返回轮次但没有正文，进度快照也无正文；当前具体进度依据其已写入主区的process_docs/0908-18与研究报告10.4/10.6及BX01/BXS01，不把文档当成重构已完成的证据。01:23记录仍在重构前快照阶段。

## 本任务收窄后的主线

1. 优先复现并修复H06错误jsonArray检查被接受/冻结、正确对象成果无法发布的问题；研究登记适用性及可追溯纠错，保持用户实际要求，不允许任意放宽。
2. 再处理选错对照包留下pending导致交付阻塞：明确作废原因及计数，保留已提交发现、归档、版本绑定。
3. 在Vercel原生ToolLoopAgent中验证检查失败→修正→发布和任务恢复；暂以现有文件/归档fixture隔离测试，不重复开展真实网页大批次。
4. 内容概率/主体/关键限定的交付审查仍是质量问题，但本任务不以重写正文采集器或浏览器观察投影解决它；依赖执行器提供固定来源证据后再评估。

## 交叉模块的具体边界

| 层 | 负责内容 | 本任务当前动作 |
| --- | --- | --- |
| 浏览器执行器（BX B0–B4） | Puppeteer/ExtensionTransport、debugger连接、元素定位/输入/等待/取消；DOM正文/图片/列表/表格采集、导航身份与续读、浏览器动作回执 | 留给「浏览器执行器优化」；不改src/browser/*或另开后端实验 |
| 共用交付组件 | src/agent/harness/delivery.ts 的要求登记、复核回执、交付版本与完成核验 | 由本Vercel任务处理上述两个H06问题；现有组件可供Pi按冻结增量复用，不复制两套实现 |
| 原生Harness接入（BX B5） | 浏览器结果映射到Vercel runtime/context，区分未知动作与未执行动作、恢复调度 | 执行器负责定义浏览器契约，本任务在契约冻结后负责Vercel一侧适配；实现前明确文件/函数/源SHA，未对齐不同时编辑runtime.ts/tools.ts/context/manager.ts |
| 文件/上下文交叉处 | files/runtime.ts、context/manager.ts同时被BX B2提及 | H05固定版本readFile已完成继续复用；本轮不改这些文件，也不重写网页观察输出 |
| 前端 | UI message、消息流消费、聊天状态与展示 | 留给前端任务 |

这里是本任务依据用户指令登记的实施边界，不冒称另一任务已经回复确认。跨任务新接口或共享文件要在实际修改前对齐；无需为当前两个交付问题等待整个执行器重构完成。

## 验证、文档与集成

本轮未改产品架构/工具/schema，无需改运行图；更新主项目状态与唯一合并清单的Vercel负责行，保留BX/Pi所有者条目。文档事实与diff检查，不报告代码测试。过程文档在5977单独提交，主区正在快照捕获时不额外推进主HEAD；登记待集成，修复代码尚未开始。

## 组合预算失败补充（2026-09-08 01:29:24 GMT+8）

执行器负责人同步冻结1905文件的组合验证仍在运行，要求登记预算失败且不修改冻结副本。本任务只读核对/tmp/octopus-harness-budget.log：独立诊断1项失败/30跳过，原期望paused实际failed，抛错位置为ContextManager.prepare的工作预算保护。记录估计instructions6787、tools44962；wpWriteContent11222、wpCompilePage10860，两项合计22082。这些是本地估算值，不是provider实付token。按单项估计相加，64000扣工具、指令和10000预留约剩2251，小于6000保护阈值；精确fixedTokens仍须按实际合并序列化及tokenRatio计算。

主区runtime.prepareStep已经将activeTools过滤后的工具传给context.prepare，所以不能直接断言代码总是把所有已安装工具发给模型。后续应先复现为什么该场景会激活大schema、检查工具分组及WP schema体积，再确定最小修复；不直接扩大预算常量或删除保护。

职责：Harness负责工具选择/实际激活集合和预算计算；WordPress功能负责人负责schema设计及语义完整性；浏览器执行器保留其冻结组合验收证据。涉及runtime/context/tool-selection修改前按前述交叉边界对齐，不由执行器重写另一套预算器。本项作为组合启动阻塞候选，在继续H06交付修复前先定位；尚未自行重跑、未确认隔离快照最终结果、未改任何产品/快照代码。
