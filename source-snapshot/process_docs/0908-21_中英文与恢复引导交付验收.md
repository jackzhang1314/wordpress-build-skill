# 中英文界面与恢复引导：收尾实现与交付验收

- 时间：2026-09-08 01:33:03 GMT+8
- 触发：主要功能收尾与关键回归里程碑
- 分支：codex/agent-workspace-sandbox，核对基线953ba65；本任务源代码增量未单独提交。主区另有其他任务变动，不能用HEAD证明全部在途代码已交付。

## 完成内容

1. 2466对类型安全中英资源；跟随浏览器/简体中文/English独立设置、保存与跨入口同步。
2. 设置、模型/连接器、历史、队列/确认、技能/提示词/文件、聊天/批注/工具状态、网页引用、写作/划词/浮窗、翻译/字幕、截图/长截图、录屏/剪辑静态控制接入。内置技能标题、说明及168条输入/输出/要求元数据补齐；技能包正文、执行示例、用户自定义内容保持原文。
3. 未配置API、数据授权、站点权限、连接器配置错误提供本地化操作与稳定设置目标。未知原始错误仅放折叠诊断，脱敏并支持复制；复制失败仍可手动选择。
4. 注入函数通过纯文本参数接收资源模板，本地插值，不依赖模块闭包，不传设置对象或凭据；捕获会话期间固定语言，下次打开使用新语言。
5. 技能hover误触发已修复；新增textarea到contenteditable无障碍标签/排队placeholder同步，保留节点和草稿。

## 关键文件

src/i18n/{runtime,dom,catalog,injected}.ts；src/lib/{product-error,settings-guidance,settings-guidance-view}.ts；src/agent/settings-navigation.ts；src/agent/skills/{localization,picker,composer}.ts；src/capture/{editor,scroll,service,preview}.ts；src/recording/inline.ts。其他UI接入见模块文档。

## 验证记录（按实际阶段，不拼接旧结论）

- 01:10全量193文件1509项通过。
- 01:23再次整合194文件1517项：1500通过、17失败。7项来自长截图旧英文断言中止清理的连带失败，已修正；9项关联WP工具schema预算超限；1项table-extraction在全量压力下超时。不能将此次整合写为全部通过。
- WP预算独立复现：instructions6787 + tools44962，其中wpWriteContent11222、wpCompilePage10860；任务工作预算剩2251 < 6000。已交WP负责人封闭修复，未关闭预算保护。
- 最后UI回归：capture-scroll18、skill-composer14、recovery-localization4，合计3文件36项通过；前序transcript/settings/style4文件61项、技能相关4文件32项通过；typecheck/lint/build通过。
- CUA最新构建专用IAB62：英语320px，缺Key发送保留草稿→Configure API key直接聚焦输入框；中英文热切换保留草稿；200%文字换行、纵向滚动。原始中文会话正文未被改写。
- CUA写作IAB63：邮件式富文本获焦后显示纯文本动作，润色结果写回，关闭浮窗无残留；仅演示生成，无真实邮件或付费模型调用。
- CUA技能：插入后继续输入不自动弹出tooltip。精确hover/离开时序由自动测试覆盖。
- Gmail空回复、签名/引用排除、只读输入、DOM快照变化拒绝替换由兼容测试覆盖；未将邮件模拟器写为真实Gmail账号验收。

## 待封闭与边界

- 源码、测试与构建已封闭，详见下方最终验收。Comet原任务自行中断后，用户01:36又启动了总结任务，截至01:39仍显示正在处理及暂停任务按钮；为避免中断用户任务，本次没有重载。真实已打开页面可能仍运行旧代码。
- 录屏另一个任务已按用户要求暂停设计开发：结束态关闭/取消和网页原地框选交互不在本次静态多语言修复内，不能声称这些产品问题已解决。
- 浏览器策略禁止注入的页面不能由插件绕过；提供可执行说明，不伪装重连成功。

## 最终验收补记（2026-09-08 01:40:31 GMT+8）

- WP负责人已压缩对模型暴露的JSON schema，并保留运行时完整参数校验；原预算回归已消除。
- 当前组合全量：194个文件、1519项测试通过，耗时214.51秒；typecheck、lint、build通过。测试输出仍有browser-conditions worker结束超时告警和jsdom导航未实现提示，进程退出码0，无失败测试；不宣称测试输出零告警。
- 实机发现运行中过程标题将label函数插值为源码；已改为执行label()，增加中文/英文热切换、节点保留与不得显示函数源码的回归。另补齐消息目录与历史网页/视频引用的6项资源。
- 最后增量验证：transcript、recovery-localization、message-navigation、message-attachments、i18n共5文件58项通过；typecheck、lint和build再次通过。未将最后58项与此前1519项相加为全量统计。
- 类型检查之外，使用TypeScript checker扫描src模板插值中的可调用类型，最终无命中。
- 验收构建独立副本：output/i18n-accepted-0908-0139/，并保存阶段日志；manifest.json SHA256：3d2aaa268c6c63ada720686f942531e0b037adfe24c602070fe33cfc7ab39240；sidepanel.js SHA256：c2c63143cc97edcbacf8bd5e45fa5198e248871489e3d92accbe52512e3e4fd5。
- 未提交本任务增量、未上架发布、未重载真实Comet；不把本地构建等同用户正在运行的版本。已解除共享源码冻结，后续WordPress新阶段另行验收。
- 后续加载：用户当前Agent任务结束后，在Comet扩展管理页重新加载扩展，再刷新要使用插件的网页；避免在任务运行中重载。
