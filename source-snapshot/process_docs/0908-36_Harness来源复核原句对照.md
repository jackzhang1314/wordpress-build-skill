# H10：来源复核原句对照

- 时间：2026-09-08 02:19:23 GMT+8。
- 触发：重要交付契约调整，先记录设计和证据边界。
- 工作区/分支：5977 / codex/vercel-delivery-contract-20260907，基线f0bb0eb；目标codex/agent-workspace-sandbox，目前实现与测试在途，尚未提交/整合。

H06真实归档中，来源boundaryContext有“The likely reason”，目标第8页改成确定原因，模型却以findings=[]和笼统summary提交复核。输入并未缺少限定，问题是没有可审查的具体对照，空结论不证明忠实。旧失败证据不改写。

复用[AI SDK官方工具调用](https://ai-sdk.dev/docs/ai-sdk-core/tools-and-tool-calling)的inputSchema/execute/原生工具错误循环和当前安装类型；SDK负责结构验证与执行，不负责判断引用语义。本阶段仅增加产品来源对照规则，不增加依赖、第二个循环或模型判官，不改浏览器执行器/前端/Pi循环。

新提交的空findings必须附comparisons：sourceIndex、逐字sourceQuote/targetQuote、supported/changed/uncertain和理由。核对固定归档中的来源（含boundaryContext）和当前目标版本片段；包外/伪造/改写引文拒绝且保持pending。changed/uncertain即使findings为空仍阻止发布；已提交comparisons和发现一样不可改写，需修文件后对新版本复核。非空findings仍可直接报告无法配对的问题。旧已提交无对照的记录允许原样重试；旧pending在新代码首次提交空发现时需提供对照。

引文存在并不证明它们有关、来源可靠、限定完整或supported判断正确，仍是模型自查；不使用跨语言关键词计数冒充语义检查。只有少量原句对照不代表全文通过，不增加100%原始字符门槛。此阶段完成后需记录类型/lint/相关测试/构建与组合验证。

## 首轮验证与复审

新增约束后旧来源相关夹具明确补原句对照，范围/生命周期夹具的脚本判断不作为语义评分。第一轮2文件109项/types/lint通过。原生H10新测试首轮因测试脚本重复toolCallId触发既有历史保护，修正为每次操作独立ID后，5文件157项/types/lint/build通过（02:22:00，4.40秒）。没有关闭保护或修改产品循环来让测试通过。

原生11步脚本覆盖空结论拒绝→原句对照承认限定丢失→发布失败→修稿→新版重新复核→发布及恢复后详细对照保留；属于MockLanguageModelV4控制的原生循环，未运行新真实模型。进一步复审发现新增归档await可能允许并发复核互相覆盖，提交前增加最终状态重核并补并发回归，最终冻结验证待完成。

## 候选冻结

最终候选5文件160项、typecheck/lint通过（2026-09-08 02:24:45，5.38秒）；产品源码同02:24:09的生产build通过版本，其后只加2项作废/停止并发回归。并发提交仅一方成功，证据读取期间作废或停止后不得提交。首次类型检查指出跨await引用的状态被静态收窄；通过重新查询当前回执完成运行时检查，没有使用as any或断言绕过。

关键变动：src/agent/harness/delivery.ts、tests/harness-delivery.test.ts、tests/harness-delivery-runtime.test.ts、交付模块说明及本记录。checkDeliveries在紧凑索引之外单独分页返回reviewComparisons，保留历史恢复入口。无依赖/浏览器协议/模型循环变化；真实引文+错误supported仍可能放行的反例明确存在。下步主区组合验证与整合，之后仍需独立真实模型评测证明收益，不能把脚本响应当作模型自行发现问题。

## 主区整合完成

2026-09-08 02:26:47 GMT+8记录：源f575185e193612302bb9f6c540b0b7254f85d92d，目标codex/agent-workspace-sandbox由4fc1e7b合并至aa7b31b123e3da3114a7ee38a131e7752c08d022，保留已整合U05。整合前相关五文件及暂存区无他人修改；整合前后已有未提交tracked diff哈希完全相同，没有整包提交或删除其他工作。

主区2026-09-08 02:25:57的typecheck/lint与6文件176项通过（5.75秒），包含WordPress组合回归；这是含其他任务在途内容的工作区组合验证，并非全产品干净提交的验收。本轮没有构建主区dist、重载用户Comet、操作浏览器或运行新付费模型；来源忠实度仍需独立真实业务评测。

主架构/状态/唯一合并清单更新H10的源、目标、范围和兼容边界；共享未跟踪治理文档不整包提交。5977保留已验证的源候选f575185，不把主区尚未收束的U05/i18n依赖带回独立候选冒充可复现构建；H10源码已是主区祖先，无本阶段遗漏源码。Pi负责人已收到准确共享SHA和接口变化，浏览器执行器未改。原工作树和分支全部保留。
