# 前端同访问晚回执接受保护（U04）

时间：2026-09-08 01:36:40 GMT+8。
触发：完成局部实现与关键定向验证，先保存证据再继续完整验收。

所属：ec1d / codex/vercel-ui-streaming-20260907；基线69f530e；尚未提交。目标：codex/agent-workspace-sandbox，尚未集成。当前主区953ba65及其他任务未提交内容未修改。

## 研究与实现

读取panel、model、后台命令/订阅读取、runtime.changed、HistoryView、调用者与测试；复核安装ai@7.0.93官方ChatTransport类型及固定SDK chat.ts请求身份。SDK现有UIMessageChunk接口不提供此产品Chrome RPC/Conversation仲裁，保留宿主薄适配，无依赖/持久化/协议/执行循环变动。

captureSelection附带当前不可变Conversation引用；setConversationReply先检查访问归属，再拒绝等待期间已接收状态之后等/更旧时间戳的回执；较新时间戳仍接受。恢复读取保留Port校验；显式打开同一会话、历史初始load、暂停load、队列操作和发送成功回执统一使用此入口。发送确认仍正常确认草稿/附件与解除sending，不重发业务动作。

源码：src/agent/panel.ts；正式回归：tests/panel-session.test.ts、tests/prompt-panel-send.test.ts。更新现有计划与流式消息模块说明；主架构相应入口在候选冻结/整合时登记。

## 验证

- 修改前新增竞态回归：39通过/8失败（output/u04-red.json），失败均为预期的旧回执覆盖。
- 修改后typecheck、lint通过；定向4文件66项通过（output/u04-focused.json），包括U02/U03既有回归。build通过。
- 隔离Chromium原生侧栏正常/丢帧两场景均通过：400源事件、1000折叠工具、12次输入、滚动0、最终精确文本、刷新恢复和remend；故障场景实际丢1帧、建立2个UI连接。未用该探针宣称实际Chrome产生等时间戳RPC冲突。结果output/u04-native.log。
- 冻结源码完整回归140文件1073项通过（output/u04-full.json）；校验三项源码/测试SHA256未变化，结果绑定本候选，非主区其他任务组合。

## 边界与下一步

这是保守接受测试，不是用户实际遭遇问题的证据；runtime.changed本已递增updatedAt。仅处理同访问等待期间的新状态与晚回执，不宣称得到跨worker/跨订阅全局版本；回执先到、随后等时间戳快照仍属原接受语义。HistoryView自身重命名/置顶的changed回调未改，其后台递增时间戳契约仍适用。无连接状态文案改动。

后续完成冻结全量/侧栏验证，协调共享panel与主区冻结窗口后精确集成；用户Comet加载单独核验，不使用上一轮证据代替。

冻结：2026-09-08T01:41:28.331665+08:00。模块说明、计划、JSON证据与本记录已同步；主架构候选登记及集成状态将在源提交后记录。两处与主区i18n文案重叠的三方冲突已在output预演解决，保留本地化；尚未写主区源码。

## 整合收尾（2026-09-08T01:47:17.092439+08:00）

源码708534a（7个文件）在69f530e基线上完成冻结全量；随后仅合入正式H06/H07形成c26b5f，6文件81项与types/lint通过，再纳入H07文档形成f7c496f086ff1cb45473ca93096b8f3039be2017。主区从397e8dd快进f7c496f，索引完整匹配正式树且为空，保留所有其他任务未提交改动。

快照负责人确认旧副本已冻结/推送，不再吸收新改动；i18n确认只修transcript文案、不碰panel。主区3项源码/测试按SHA256预映像检查后写入，panel两处多语言行冲突仅叠加setConversationReply，保留原翻译。4项模块/计划/证据/过程文件三方合入。没有整体stash/reset或提交他人内容。

主区8文件123项、types/lint通过；正常/丢帧原生侧栏再次验证400源事件、1000工具、12次输入、滚动0、精确文本、刷新与remend。主区原生构建在本任务output/u04-main-build隔离副本，未修改主区dist或重载Comet，避免打断用户正在运行的任务。

失败如实保留：主区初次types被连接器seo-service-view.ts(37,277)的TS2554阻塞，由负责人修正参数文案后通过；临时构建初次漏复制wordpress-site目录，出现ERR_MODULE_NOT_FOUND，补齐现有目录后构建通过。原生副本早于无关连接器类型修正，主区types晚于修正；分别报告，不拼成整份主源码冻结验收。U04三文件指纹在组合回归、侧栏和合入前后相同。

文档影响：现有流式消息模块、同步计划、JSON证据和本记录已更新；主ARCHITECTURE追加RPC显示接受的现状，PROJECT_STATUS与唯一清单仅更新前端条目。无新运行入口、依赖、协议或安装步骤，README无需变更。连接状态提示和跨worker全局版本仍为后续。
