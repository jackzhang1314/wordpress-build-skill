# Hostinger 公网预览站验收

站点：https://mediumblue-quail-505146.hostingersite.com/ 。2026-09-20 实测，公开 mock/noindex 预览，视觉质量不因此视为用户认可。

- report.json：远端 3357 文件哈希、6 媒体、19 产品、2 询盘实际记录、运行版本和清理结果。
- routes.json：7 个关键业务页面与正确 404、唯一 title/H1、无本地资源地址。
- assets.json：含产品分类页等 8 个页面的 33 个同源静态资源，HTTP 状态和 MIME 均通过。
- admin.json：管理员登录、产品编辑页、ACF/Fluent Forms/Rank Math 后台入口。
- browser.json：移动导航、首页表单和产品询盘弹窗各提交一次，产品上下文正确，无运行时 JS 错误。
- PNG：公网浏览器截图。home-desktop 等待首屏图片真实解码后重拍；截图不代表设计质量获批。

已创建目标文件/数据库备份，保存于主机公网目录外；没有进行远端恢复演练。邮件通知关闭，未宣称真实邮件送达。临时任务和执行文件已清理，测试询盘两条保留为验收记录。管理员凭据、SQL 与私有迁移回执不进入此目录。

用户随后报告旧首页，确认原 HTML 被设置一周缓存。已修正预览响应为 no-store 并启用 cacheless；cache-policy.json 记录两个解析地址的有效 TLS/新首页/缓存头。旧浏览器副本需一次强制刷新或使用带部署参数地址，不能把单次新会话正常当作旧缓存已被清除。

补充闭环：用户已确认无痕浏览器访问原域名正常。preview-gate.json 的 4 个不带参数页面均通过身份/预览缓存检查；links.json 的首页、设备、行业、博客入口发现的 43 个站内链接均 200 且同源。这不是全站无限深度爬取，也不替代页面内容/设计评审。
