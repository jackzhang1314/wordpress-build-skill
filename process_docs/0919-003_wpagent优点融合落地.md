# 0919-003 wpagent 优点融合落地

- 时间：2026-09-19 07:45 Asia/Shanghai
- 触发原因：用户确认把 wpagent 项目所有优点与启发点全部借鉴并融合进本方案。

## 主要工作

1. **docs/15-wpagent优点融合方案.md**：11 个优点逐项融合映射（架构转译原则）、本轮落地明细、新项目必装清单（ACF + Fluent Forms + SEO）、拒绝清单与理由、生产部署约定、验收清单。
2. **SKILL.md 管线接入**：阶段 4.5 HTML 预览门（`design/preview-[page]-v{n}.html` + `data-field` 对齐 ACF 字段，QA 通过才转模板）；阶段 5.7 页面模板版本化命名（`page-*-v{n}.php` 不覆盖旧版）。
3. **references/design.md**：补「视觉避免清单」（8 条，重复问题双倍扣分）与「版本历史」节（v1/v2 演进记录）。
4. **主题层机制**：`functions.php` 加 `acf/` 目录自动加载器；落地完整示例闭环——`page-contact-v1.php`（联系页自由模板）+ `acf/page-contact-v1.php`（成对字段组，location 绑 `page_template`，`show_in_rest`）；Fluent Forms 短代码检测 + mailto 优雅降级。
5. **scripts/deploy-lab.mjs**：分层部署（file 单文件热更 / theme / plugin / backup / all 含冒烟），实测 all 通过（备份 + 同步 + 4 路由 200）。
6. **联系页实测**：页面 74 切换 `page-contact-v1.php`，REST 填 9 个 ACF 字段，前台渲染验证 + 桌面截图（`.lab/design-02/qa-contact-v1.png`）。

## 验证结果

- `npx eslint scripts/deploy-lab.mjs`、`npm run typecheck`、`npm test`（23/23）通过。
- `deploy-lab.mjs all`：备份 → 主题/插件同步 → 冒烟全绿。
- 联系页自由模板 + 成对字段组渲染正常，降级路径符合预期。

## 遗留问题

1. Fluent Forms 未在 lab 安装，表单短代码路径未实测（模板有守卫与降级，真实站点装机后回归）。
2. 生产 SSH 部署脚本等真实服务器后实现。
3. 预览门 token 检查工具化（lint 规则）为下一步迭代项。
