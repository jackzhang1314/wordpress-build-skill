# 架构上手与模板能力核对

时间：2026-09-20 05:08 Asia/Shanghai（UTC+08:00）。

触发：用户要求梳理架构与进度、指导新用户快速建立关键页面，确认后台产品/分类是否可以选择多套自定义模板。

工作：核对主题、CPT/Taxonomy、ACF 定义、content-plan 输入、区块模板适配器、历史验收；对现有 9464 实例只读查询 REST OPTIONS，不修改内容或配置。查阅 WordPress 官方模板和层级文档。新增 docs/GETTING-STARTED.md，包含架构、能力矩阵、Codex 首次建站提示词、逐页后台操作、模板需求边界及建议实施方式。README 和 ARCHITECTURE 增加入口/缺口说明，Skill theme-code 补充多模板能力判断与验证规则。

结论：产品/分类已有内容维护能力；普通 Page 有两个命名模板。产品与分类分别使用统一布局，尚无多模板选择器/库；分类不能直接套用普通 Page 模板功能。content-plan 未接受 template 字段，不能将 WordPress 的 REST template 属性当作工具已支持。AI 生成模板并供用户选择，与后台可视化设计新模板是不同交付目标。本轮没有实现模板切换，也没有改变视觉。

证据：docs/acceptance/0920-template-audit/capabilities.json（只读接口）、source.json（目标源码哈希）。现有后台 ACF 保存证据与完整新产品所有步骤的后台点击验收明确区分。

验证：指南和入口链接存在、git diff --check、Skill quick_validate 通过。纯文档与只读审计未运行不相关代码测试。没有暂存、提交或发布；既有未提交内容保留。

下一步建议：沿当前 PHP 路线先实现两套产品模板和两套分类布局，后台选择、保存、切换、默认回退与内容/URL/询盘不丢失一起验收。若用户要求后台自由可视化设计模板，则单独评估区块路线，不隐式扩大本轮实现范围。
