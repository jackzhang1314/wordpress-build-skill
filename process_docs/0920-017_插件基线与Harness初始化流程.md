# 插件基线与 Harness 初始化流程

时间：2026-09-20 08:17:45 GMT+8（Asia/Shanghai）。

用户要求梳理必须安装的插件，并在 WordPress 初始化时安装配置。本轮建立 config/wordpress-plugins.json：ACF Free、Fluent Forms Free、Rank Math Free、项目业务插件；邮件、防垃圾、备份/恢复、缓存图片、多语言按能力和环境分开处理，不堆叠插件。

start.mjs 已调用 plugins.mjs 读取清单，检查所有版本后按序激活，保存回执；增加 plugins-check.mjs 和 npm run test:starter:plugins。ACF 下载指向厂商官方固定版本。Skill、项目 AGENTS、架构/使用说明同步。

验证：两个现有隔离实例插件/基本配置检查通过，3 项新测试通过，全套 38 测试、lint/build/diff-check 通过。证据 docs/acceptance/plugin-harness/。本轮只读核对站点，没有重建/更新/重置已有数据；安装流程未在新容器从零实跑，下一阶段可做干净安装复验。生产投递、防垃圾和恢复等条件仍需按目标环境验收。
