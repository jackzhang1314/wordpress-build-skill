# CMS 后台完整验收

时间：2026-09-20T19:57:14.627420+08:00（Asia/Shanghai）

## 范围

用户明确要求所有页面编辑、上传、图片修改等 CMS 功能。线上盘点 19 页面、19 产品、11 行业方案、9 文章、5 产品分类、17 模板、2 模板部件、6 媒体。先保存私有完整 REST 内容快照，创建/删除专用草稿验证写入和前台读取，再以隔离 Playwright 会话测试实际 UI；不覆盖线上业务数据。

## 已发现的问题（保留诊断）

- 自动脚本普通页面错误使用 p 查询返回 404，改为 page_id；HTML 正文断言误以为段落无 class，WP 7.1 实际加 wp-block-paragraph，改为允许段落属性。初始失败矩阵单独保留，不把脚本失败视为网站失败。
- ACF 6.8.9 在 preview=true 时会选择最新 revision；REST 先保存核心内容后写 ACF，导致最新 revision 尚无最新字段，直接拼预览 URL 看见空字段。检查已安装官方代码确认该分支。实际后台 ACF 输入/保存草稿后 preview=true 的型号和规格正确；批量保存检查改为读取已保存草稿，不冒充 ACF 编辑器预览已覆盖。
- Playwright 在较小窗口点击 Meta Boxes 中心时被核心 resize separator 拦截；扩大视口并用键盘聚焦 Enter 打开成功，保留为核心 UI 可用性观察，不用 force click 隐藏。
- 错误请求 wp_navigation REST 路由返回 404，须以 /types 声明的 rest_base 路由为准。一次 shell URL 未引用导致 zsh glob 错误，改为引号包裹。一次搜索不存在 model.php，真实入口为 site-model.php。

## 进行中

全内容草稿克隆回归、实际 UI 的产品字段/主图上传替换、分类与模板选择、导航/站点编辑器与 SEO/询盘管理。测试草稿清理逐项记录。

## 已定位的实际缺陷与修复

1. 联系页 enquiry 模板未输出 post-content，页面正文保存后不可见。已在设计生成器和模板补入原生 post-content，重建 Skill 49 文件样板、同步本地并定点上传线上；专门克隆联系页复测通过。新增 template-editability.test.mjs 覆盖内容模板不能丢弃正文的契约。
2. 产品模板选择器原硬编码只允许 product-standard/product-editorial。实际创建 harness-cms-template 后无法选择。改用 WordPress 真实 post_types / source / has_theme_file / is_custom 元数据，允许显式适用于产品的模板及用户新建自定义模板，继续排除未声明的层级模板。primary/reuse 两个本地站功能回归通过，包含四类模板筛选用例与 PHP 语法检查；正在做线上 UI 新模板选择复验。
3. 分类预览两次 IncompleteRead，写入和清理完成；保留初轮网络失败证据，只对只读 HTML GET 加有界重试，五分类两布局复测通过，不重放未知写入。
4. 媒体对话框实际标签为 Alt Text，初次等 Alternative Text 超时；修正标签后上传/替代文本/图注/主图替换成功。普通正文 Image 的内部 img 被编辑器 figure 拦截是选择区块机制，改为操作 Block: Image 容器，不使用 force click。

## UI 结果补充

产品 UI：标题、五个 ACF 字段、上传 JPEG/替代文本/图注、替换主图、Standard→Editorial→新建自定义模板均已保存并回显。普通页面 UI：标题、正文段落、图片块替换均已保存并回显。Site Editor：自定义模板标题块和独立导航菜单文本一起保存，后端与前台均验证成功；预期 Site updated. 提示未出现导致等待超时，保存本身成功，最终以持久化回读为证据。分类描述实际由 Rank Math 增强为 TinyMCE，不是普通 Description textbox，后续按实际富文本 iframe 操作。

## 收尾验证（2026-09-20，Asia/Shanghai）

58 个实际编辑器只读打开通过；58 条内容克隆覆盖在联系页修复后全部通过。五分类两布局回归通过。产品与行业分类关系保存/渲染通过，分类 UI 名称/描述/布局及 Rank Math 标题描述持久化通过。独立模板和导航在 Site Editor 保存并由产品前台输出通过。发布后匿名访问正文与图片通过，实际移入回收站及恢复完成；询盘管理可见原有两条测试记录。

发布完成面板遮挡 Move to trash，初次操作超时；重新加载关闭面板，再完成确认对话框。Restore 链接的可访问名称包含内容标题，精确匹配 Restore 超时，按真实 aria-label 定位后完成。Playwright raw 模式未打印返回内容，后续采用正常结果输出。以上属测试定位问题，保留而不冒充网站缺陷。

新增正文可编辑契约测试；相关 6 个 node tests、npm run lint、两个本地站完整功能检查通过。Skill 样板重新生成并同步；线上定点更新两文件，原发布包全量哈希与恢复快照不覆盖这两项后续更新。经验回写 verification.md/theme-code.md，用户操作表回写 GETTING-STARTED.md。最终清理与原数据差异见 docs/acceptance/hostinger-cms/report.json。

边界：管理员桌面浏览器，不包含全部角色、移动后台、所有插件配置；正式域名、真实邮件、商业事实和视觉认可仍待后续，不把 CMS 功能验收当成商业站最终验收。

最终核对：2026-09-20 20:20:22 +08:00，19 页面 + 9 文章 + 19 产品 + 11 行业方案 + 5 分类仍在，原业务字段差异为零；本轮测试对象全部清理。回收站恢复为草稿，正文保留，不能误报恢复即重新发布。
