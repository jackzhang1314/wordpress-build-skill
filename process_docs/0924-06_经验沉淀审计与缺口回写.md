# 0924-06 经验沉淀审计与缺口回写

- 时间：2026-09-24 20:45 Asia/Shanghai
- 触发原因：用户要求确认本轮问题是否已沉淀回 Harness，并区分 Harness/流程缺陷与其他原因。
- 审计结论：
  - 已沉淀为机器门禁：`wordpress-classes`、`component-duplication`、`zero-media`（本地）、`acf-binding`、routes、FF `submitButton`、blank media remote verify、screenshot 命令。
  - 已沉淀为规范：命名空间全局类、组件化 classic theme、Starter 空媒体不变量、全新空站验证矩阵、Fluent Forms 可渲染结构、worktree/session 所有权。
  - 已保留过程证据：Fresh-site 500/rollback、FF 空渲染、69/69 截图、真实表单提交与数据库入库。
- 根因分类：
  1. **Harness/流程缺陷**：缺少 namespace 静态门禁、缺少组件重复门禁、缺少空媒体远端校验、缺少 FF 渲染/提交闭环要求、缺少全路由三端截图命令。已补。
  2. **实现缺陷**：`new WP_Query()` 漏全局反斜杠；手写 FF 表单少了 `submitButton`；Contact 短代码没有真实渲染校验。已修复并加回归。
  3. **产品/架构理解不足**：Starter 不是普通站点，必须保持零媒体、中性命名、尺寸占位和组件化。已写进 architecture/media/verification。
  4. **外部技术行为**：WordPress 命名空间解析、Fluent Forms 内部字段/meta/submitButton 结构、Hostinger/LiteSpeed 缓存。这些不是 Harness bug，但已固化为版本相关经验和检查要求。
  5. **并行协作流程问题**：两个 session 曾同时写同一 worktree，导致组件定义被覆盖。已写明单写者规则、分支/HEAD 前置核对、收编前 diff 归属和全量验证。
- 本轮新增回写位置：
  - `.agents/skills/wordpress-builder/references/verification.md`
  - `.agents/skills/wordpress-builder/references/media.md`
  - `.agents/skills/wordpress-builder/references/classic-acf-default.md`
  - `.agents/skills/wordpress-builder/references/orchestration.md`
  - `harness/lib/verify.mjs`
  - `tests/harness/verify.test.mjs`
- 遗留增强：
  - 浏览器表单提交目前是手动真实验收；后续可把“渲染 `<form>` + 测试提交 + DB 增量”做成独立 `verify-form` 命令。
  - 截图门禁目前校验文件存在/大小，后续可加视觉基线 diff。
  - Fluent Forms 版本相关结构应记录版本矩阵，升级时重新验证。
