# 区块代表页骨架整合与真实验收

时间：2026-09-20T07:15:07+08:00（Asia/Shanghai）

触发：用户同意按最新架构、SEO 与项目 AGENTS 规范落实新骨架并测试。

## 实现

新增 examples/b2b-block-starter 独立项目，保留原 HONGDA 与 A/B 实验。原生区块主题、两套产品模板、分类布局后台字段、Pattern、官方 ACF 绑定、业务插件动态块和询盘目标配置整合。项目 AGENTS.md 记录真实目录与命令。新增构建步骤，将 34 个主题/插件源码文件及 manifest 打包进 Skill assets/block-starter；不打包 seed、私有凭据或实验 SMTP。

新增 start/sync/check/browser/screens/seo/inquiry 脚本与 npm 入口。9490、9491 各自全新数据库：设备样板与小型泵数据集；9492 为隔离 Mailpit。没有复制旧数据库。实测版本和源码哈希见 docs/acceptance/b2b-block-starter/。

## 验证与修复

两站功能回归、真实后台模板/ACF/分类编辑、首页/Pattern 保存、18 份区块源解析、8 个桌面/手机页面检查及移动菜单通过。正文/主图的编辑器数据接口加真实 Save 与完整媒体选择器 UI 测试区分记录。询盘必填、无效产品拒绝、一条成功入库及一封 Mailpit 收件通过。

8 类 SEO 页面验证，核心 sitemap 的产品与分类子图检查，mock admin 作者归档 noindex；临时公开索引模拟在 loopback 执行并恢复 noindex。修复目录描述缺失、筛选页双 robots、sitemap 不含分类的模式配置问题。修复 docker cp 路径错误及后台测试依赖视口/面板状态的问题；失败过程在验收 README 保留。

npm run typecheck、npm run lint（含新增脚本）、npm test 35/35、npm run build、Skill quick_validate、git diff --check 通过。13 份变更入口文档的本地链接通过，官方 10 模块完整性通过。新增占用端口预检实测在创建运行目录之前拒绝，无新资源产生。工作区原有未提交修改保留，未提交 Git。

## 沉淀与边界

更新 Skill、reference-site、theme-code、seo、README、目标架构、实现现状和上手文档；项目状态以验收证据为准。

当前为代表页骨架，不是全部商业站完成。后续补行业/About/文章等完整页面、真实内容与视觉验收、子目录和全部路由适配、生产主机及整机恢复。编辑器 global-styles iframe 警告仍存在，保存与区块验证通过；未将警告隐去。未做新 Codex 任务的独立使用评估，泵环境仅证明新数据集和干净安装复用。真实邮件投递、Google 收录与现场 CWV 未覆盖。
