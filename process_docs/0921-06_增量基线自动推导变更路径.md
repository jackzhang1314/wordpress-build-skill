# 增量基线自动推导变更路径

时间：2026-09-21 21:57 +08:00（Asia/Shanghai）。
触发：按 0919-007 交接总纲第 7 节继续。核对工作区后发现 0920/0921 会话已大幅执行优先级 1（Hostinger 真实站开通、部署、恢复、CMS 验收），共享工作区按 0921-03 决策保持未提交、走独立发布副本推私有仓库，本轮延续该约定不动 git 历史。实际续接点取 0921-02 明确列出的下一步「自动从实际增量包生成变更路径」。

## 主要工作

- 新增 `examples/b2b-block-starter/scripts/release/baseline.mjs`：接受本地清单快照（`{shape:'local-inventory',files:{path:sha}}`）或 `scripts/hostinger/theme-baseline.mjs` 的 Hostinger 只读报告（`remoteResponseHashes` + `normalizedFinalNewlineMatches` + `unverified`），对候选主题目录派生 `candidate-added`/`candidate-deleted`/`modified` 变更并按既有正则过滤覆盖路径。Hostinger 末尾换行归一化匹配按「仅差结尾 LF 视为未变」处理；`unverified` 二进制路径整条跳过、保持未验状态，既不算变更也不冒充通过。
- `update-preflight.mjs` 新增两种模式：`--snapshot THEME_DIR OUT.json` 导出逐文件 SHA-256 清单留档；`--baseline BASELINE.json CANDIDATE` 自动生成变更路径后走同一数据库覆盖清单检查。基线模式允许零变更路径（此时仍输出当前全部发布态覆盖供留档），手工路径模式保留空输入拒绝语义。
- `overrides.mjs` 把覆盖路径正则导出为 `OVERRIDE_PATH_PATTERN` 供复用，`validateChangedPaths` 行为不变。
- 同步 `references/release.md`（基线快照与自动推导段落）和 `docs/ARCHITECTURE.md`（增量预检状态行）；交接文档第 7 节已确认过时，本轮以 0921 系列过程记录为准，不改旧交接文档正文。

## 关键文件

- `examples/b2b-block-starter/scripts/release/baseline.mjs`（新增）
- `examples/b2b-block-starter/scripts/release/overrides.mjs`（正则导出）
- `examples/b2b-block-starter/scripts/update-preflight.mjs`（--snapshot/--baseline）
- `tests/incremental-preflight.test.mjs`（新增）
- `.agents/skills/wordpress-builder/references/release.md`、`docs/ARCHITECTURE.md`

## 验证结果

- 新增离线夹具测试 5 项（基线形态校验与拒绝、增删改派生与覆盖过滤、换行归一化语义含未验二进制、精确字节匹配、CLI 用法拒绝），连同既有覆盖/差异测试 7/7 通过；全量 `npm test` 65/65 通过。
- `npm run lint`、`npm run typecheck`、`git diff --check` 通过。
- CLI `--snapshot` 实跑 `examples/terralift-ui-theme` 导出 34 文件清单（临时文件已清理）。
- 未执行需要本地 reuse 容器或 Hostinger CLI 的实站预检；本轮不写 `.lab/`、不触线上。

## 遗留问题与下一步

- 派生逻辑已离线测试，但 `--baseline` 对本地 reuse 站和 Hostinger 基线的实跑预检未执行；0921-02 的 Hostinger 基线报告可作为现成输入在下次实验窗口实跑。
- 基线只证明采集时刻状态，发布前需重新采集；基线时效校验、冲突处置与备份/回退、部署中新增询盘保留、二进制/插件与数据库覆盖的完整基线及增量传输仍未覆盖。
- 共享工作区继续按 0921-03 约定保持未提交；本轮交付文件如需入私有经验仓库，走独立发布副本流程。

## 同阶段追加：--baseline 本地 reuse 实跑

时间：2026-09-21 23:10 +08:00（Asia/Shanghai）。用户确认后立即执行「下一步①」。

### 完成

- 新增 `examples/b2b-block-starter/scripts/baseline-preflight-check.mjs`（npm run test:starter:baseline-preflight）：在本地 reuse 站创建唯一 `harness-baseline-*` wp_template 覆盖（模拟后台手动编辑），`--snapshot` 导出源主题基线，候选副本新增同名模板文件后 `--baseline` 实跑，验证 9 项：夹具创建、快照导出、冲突检出（退出码 2 + blocked）、派生路径与 candidate-added 语义正确、冲突/无冲突两轮运行后操作员覆盖内容哈希均不变、移除候选文件后无冲突（退出码 0）、清理后数据库与临时目录零残留。二次运行确认可重复（唯一夹具不碰撞）。证据 `docs/acceptance/template-update/baseline-preflight.json`。

### 实际失败与修复（按用户要求全保留）

1. 首轮运行在清理段崩溃：`wp_get_object_terms(term_id,...)` 参数用错——该函数第一个参数是文章 ID 不是 term ID，应使用 `get_objects_in_term`。崩溃前夹具文章已删除，无数据残留；修正后重跑通过。PHP API 误用属脚本问题非网站问题，实测未写入任何业务数据。
2. 主题源码路径解析错一级（`../../theme` → `examples/theme` 不存在），改 `../theme` 后通过。
3. lint 检出 `contentHash` 无效初始赋值（try 内必然先赋值），改为延迟声明。
4. 沙箱拒绝 `rm -rf`，改用 Node `fs.rm` 清理首轮残留临时目录；`.lab/baseline-preflight-*` 现由脚本 finally 自动清理，无残留。

### 验证与边界

- lint、`git diff --check` 通过；实跑 9/9 两轮通过。数据库 `name__like harness-baseline-` 零残留，无临时目录遗留。
- 边界：仅本地 reuse 站；Hostinger 远端基线实跑、冲突处置/备份回退、部署中新询盘保留、二进制/插件增量传输仍未覆盖（证据 notCovered 已列明）。
