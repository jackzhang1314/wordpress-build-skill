# 0919-005 架构全面审计与修复

- 时间：2026-09-19 10:00 Asia/Shanghai
- 触发原因：用户要求对当前 WordPress 方案做全面全局审计。

## 审计执行

1. 子代理：主题代码逐文件审计（安全/性能/规范/i18n/兼容性/边界），返回 28 项发现。
2. 主会话并行：插件层、工具链凭据处理、运行时 SEO/性能、仓库卫生、DB 覆盖检查。

## 已修复（当轮 17 项）

1. P0 级 SEO 缺陷：全部自由模板缺 `<title>` → `add_theme_support('title-tag')`。
2. header 硬编码 `127.0.0.1:9462` → 相对路径导航（实测：navigation-link 无 url 属性时不渲染 href，需回填相对 URL）。
3. CTA 路由统一 → `tl_page_url()` helper。
4. ACF 停用 fatal → `tl_field()` helper。
5. ABSPATH 守卫 12 文件。
6. `acf/page-cases-v1.php` 补齐。
7. 首页 N+1 → 分组查询；teaser 查询加 `no_found_rows`。
8. 搜索范围扩至产品/案例 + 分页块（`postType: any`）。
9. 对比度 → `--tl-signal-text`（约 4.9:1）用于按钮/小字。
10. skip link + `#tl-main`（8 模板）。
11. 首页 hero 图直出原图 → attachment large。
12. hero alt 语义（attachment alt 优先）。
13. 插件 status 版本 0.1.0→0.2.0 同步。
14. 分类 rail `aria-current` + 分页 `aria-label`。
15. mailto/tel `esc_url` + WhatsApp wa.me 链接。
16. product-dynamic 标题层级修正（eyebrow 改为绑定段落）。
17. fontDisplay swap / color-mix 回退 / prefers-reduced-motion / `get_term_link()` 防御 / textdomain 加载。

## 完整报告

见 docs/16-架构全面审计报告.md：六维度结果矩阵、修复状态、剩余 P2 待办（i18n 包装、字段名前缀迁移、字体 license、内容统一、产品详情双真源决策）。

## 验证

8 路由 200；title/搜索命中/导航 href/skip-link 逐项验证；`npm test` 23/23、typecheck、lint 通过。

## 遗留

见 docs/16 第 4 节；i18n 与字段名前缀涉及数据迁移，留待真实站点实施。
