# 0917-001 挖掘机原生方案演示验收

## 时间与触发

2026-09-17 19:50 Asia/Shanghai。用户要求继续研究并测试 WordPress Skill，梳理原生方案、执行测试，并用外贸 B2B 挖掘机题材做演示。本机仓库无前次提交，全部工作区文件仍属本阶段开发内容。

## 主要工作

- 复核 Skill 目录、README、references、source 与测试覆盖，确认当前方案核心是受控核心块、REST 计划/回执、任务日志、发布状态分离、meta 绑定、Query Loop 与专用 block template。
- 启动 `.lab/` 隔离 WordPress，构建 Skill 后用虚构企业 TerraDig Export 做完整演示：doctor、媒体上传、CSV 导入 3 条 `oct_product`、发布产品、生成 4 个页面、浏览器改稿、发布、设置首页、更新 header/footer 导航、创建 `single-oct_product` 专用模板。
- 用匿名浏览器复查桌面/手机、图片、链接、手机菜单、单一 H1、横向溢出和产品 meta 显示；把发现的三/四列窄栏、流程阅读顺序、旧测试产品混入目录、产品详情继承文章模板等问题分别改稿或用专用模板解决。
- 新增 `docs/08-挖掘机原生方案演示验收.md`，更新 `docs/07-CMS字段与导入验收.md` 的历史缺口指引，并在 content reference 中补充专用 CPT template 流程。

## 关键文件

- 演示源：`.lab/excavator-source/company.md`、`products.csv`、`product-import.json`、`site-plan.json`、`single-product-template.json`。
- 任务回执：`.lab/excavator-demo/`。
- 已提交证据：`docs/acceptance/0917-excavator/`；完整本机任务数据：`.lab/excavator-demo/`。
- 验收文档：`docs/08-挖掘机原生方案演示验收.md`。

## 验证

- `npm run typecheck` 通过。
- `npm run lint` 通过。
- `npm test` 通过 22/22。
- `npm run build` 通过。
- 隔离站实际 WordPress 7.1、PHP 响应头 8.5.8、Twenty Twenty-Five 1.5。
- 四个发布页和 3 个产品详情匿名可访问；页面站内唯一 URL 24 个全部 HTTP 200，无跳转。
- 首页图片实际加载；四页单一 H1；390px 无横向溢出；手机菜单打开/关闭正常。

## 遗留

- Query Loop 仍无 taxonomy/term/search 过滤；本次通过显式处理测试数据解决混入问题。
- HTTPS、经典主题、真实客户站、表单邮件送达、SEO 插件、证书材料和多语言未验收。
- 专用 `single-oct_product` 只处理默认 hierarchy，不能假设覆盖单独模板或更具体 hierarchy。
- 仓库整体仍是未提交工作区，未创建 Git 提交。
