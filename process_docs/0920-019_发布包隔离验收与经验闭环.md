# 发布包隔离验收与经验闭环

时间：2026-09-20T08:43:23+08:00（Asia/Shanghai）。

触发：用户同意继续发布包和空白安装，并要求将探索错误、纠正和有效做法沉淀为 Harness。分支 main，HEAD a49821fa88311b5fa59f4d0298b8a6f5b71bd7a1，保留此前未提交工作；未暂存或提交。

## 工作与归属

- 新增 examples/b2b-block-starter/scripts/release.mjs 及 release/files.mjs、sanitize.php，提供当前 mock 区块站的私有打包、隔离数据库清理、完整归档安装与恢复验收。
- npm run test:starter:release 接入项目命令；tests/release-package.test.mjs 覆盖敏感/越界路径、符号链接和文件清单篡改。
- 根 AGENTS.md 新增“将探索成果沉淀为 Harness”，同步客户模板；verification.md 加入经验闭环和本轮防复发规则。Hostinger 规范、架构与上手文档更新真实进度。
- 最新证据 docs/acceptance/b2b-release/run-Lpa0jf；完整 SQL/媒体/凭据仅 .lab 私有目录。未改原预览内容，未连接主机或对外发送邮件。

## 失败原因与沉淀

1. MySQL 初始化临时进程响应 ping，实际认证未就绪 → 改为认证 TCP SELECT 1，完整复跑通过。
2. 插件依赖附带隐藏开发配置 → 只清理发布副本中明确的 13 个元数据条目，未知隐藏文件/路径仍拒绝；逐文件验证安装哈希。特例与当前依赖版本绑定，不作为通用删除所有隐藏文件的规则。
3. 邮箱总数包含安装设置通知 → 提交前取基线，核对新增 2 封及收件人、提交邮箱。数据库实际新增 2 条，无重复提交问题。

## 验证

- 最新完整集成通过：3359 个归档文件；安装/恢复各 3357 个文件哈希、6 个附件、7 条路由；ACF model 修改、两套模板、两次浏览器询盘与 SMTP 收件；11 张逻辑表恢复一致，2 条询盘保留，容器重建数据保持。
- npm test：40/40；lint 通过；修改测试断言后再次运行两项相关测试通过；git diff --check 通过。PHP 清理脚本在实际 PHP 8.3.33 执行通过。
- 未重新评审视觉、所有后台 UI、完整 SEO/性能；未声称 Hostinger 生产或外部投递验收。

## 下一步

实现 Hostinger 只读预检与测试站首次部署。实际账户/套餐、SSH、域名和权限仍待配置；本轮参考包不作为可公开的真实商业资料，也不作为旧站兼容或任意数据库脱敏工具。
