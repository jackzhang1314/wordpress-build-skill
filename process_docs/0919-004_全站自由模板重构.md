# 0919-004 全站自由模板重构

- 时间：2026-09-19 08:40 Asia/Shanghai
- 触发原因：用户要求以最高标准对整套 skill 与整个演示站做全局优化重构。

## 主要工作

1. **全站模板切换到 D 模式自由 HTML**（主题 v0.4.0）：
   - 新增 `page-home-v1.php` + `acf/page-home-v1.php`（hero/CTA/4 组 stat 为 ACF 字段；分类展示、精选机器、案例 teaser 实时 WP_Query）
   - 新增 `front-page.php` 委托页（实测 `get_front_page_template()` 忽略自定义模板，只认 front-page.php；且区块模板 `front-page.html` 压过 PHP，已删除）
   - 新增 `archive-oct_product.php`（全机目录：hero + 分类 rail + 卡片网格，替代旧区块归档）
   - 新增 `single-oct_case.php`（案例详情自由模板）与 `page-cases-v1.php`（案例列表）
   - 新增 `page-landing-v1.php` + `acf/page-landing-v1.php`（通用落地页：暗 hero + 正文 + 可关 CTA 带），批量应用到 company/industries/faq/support/payment/dealer/oem/quality/equipment 共 9 页
   - 删除 `templates/front-page.html`、`templates/archive-oct_product.html`、`templates/single-oct_case.html`
2. **内容刷新**：6 个产品与 3 个案例的摘要从旧 placeholder 免责文改为具体卖点文案。
3. **首页 ACF 数据**：hero 文案/图/CTA + 4 组 stat（1,200+ shipped / 30+ markets / 15–30 days / 2 yr warranty）经 REST 写入。
4. **CSS 扩展**：`tl-fx-home-*`（stats 条、分节头、案例横卡）约 100 行，全部复用 token。
5. **文档**：docs/14 增补 §11 全站重构实测（front-page 特殊性、模板覆盖清单、PHP foreach 解构语法教训）。

## 视觉 QA

桌面 1440：首页（hero+stats/分类/精选/案例/CTA 五段完整）、products 归档、cases 列表、case 详情、company landing、contact 页全部过检（截图 `.lab/design-03/`）。移动 390：首页图片前置、汉堡菜单、全宽 CTA、无横向溢出。

## 验证结果

- `npm test` 23/23、typecheck 通过；`deploy-lab.mjs all` 冒烟 4 路由 200。

## 遗留问题

1. equipment/company 等页的旧 block pattern 正文（橙色强调卡）与新自由模板并存，视觉不完全统一——内容层优化留待内容轮次。
2. 全站审计子代理超时被关闭；本页视觉证据由主会话截图覆盖，未生成独立评分卡报告。
3. search/404/index/single 兜底模板仍为旧区块版（低可见度，未动）。
