# GitHub 私人仓库整理与推送

时间：2026-09-21T06:33:50.685780+08:00（Asia/Shanghai）。
触发：用户要求创建专门的私人 GitHub 仓库，保存完整 WordPress build skill 经验。

目标：jackzhang1314/wordpress-build-skill，PRIVATE。采用独立发布副本与新的 Git 历史，不整体提交共享工作区；源码、AGENTS、Skills、references/playbook、研究/过程文档、测试、插件配置及上游许可证一起保存。

排除：本机认证/浏览器会话、.lab、.wordpress-builder、node_modules、数据库归档、验收截图等。示例站 content/media 产品图片保留。部分历史 Markdown 的截图链接只在原工作区可用，远端快照不声称包含图像证据。构建生成 wp.mjs 由 npm run build 重建。

验证：独立副本 npm ci --ignore-scripts、typecheck、lint、build 通过；build 校验 10 个官方模块和 49 个区块样板文件。第一次完整测试 50/51，一项就绪测试短超时导致错误消息断言失败；未改代码复跑 51/51 通过，保留为间歇性测试问题，未宣称根因已修复。31 个本地已知凭据值扫描零命中，常见令牌/私钥特征扫描无命中。第一次恢复示例图片的命令在发布副本下错误取 cwd 导致 FileNotFoundError，改绝对源路径后完成。

远端创建/推送及可见性以实际 GitHub 回执为准。原工作区未提交修改保持原状；后续不能把原 Git 历史直接强推到这个干净快照仓库。

检查补充：初次导出完整新历史的 staged diff 检出上游/历史资料已有尾随空格；保留上游原文和哈希，不格式化固定版本依赖。发布记录复制再次使用错误 cwd 已纠正为绝对路径。

远端验收完成：GitHub 返回 isPrivate=true / visibility=PRIVATE，默认分支 main；首个快照提交 4082bf09ee8098549b14205f7908317961f286c6 与远端 refs/heads/main 一致。仓库 https://github.com/jackzhang1314/wordpress-build-skill 。独立发布副本位于 .wordpress-builder/github-export/wordpress-build-skill，origin 仅配置在该副本；原共享工作区没有整体暂存或提交。
