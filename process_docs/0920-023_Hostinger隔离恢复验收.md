# Hostinger 隔离恢复验收

时间：2026-09-20T19:49:21.721709+08:00（Asia/Shanghai）

用户要求继续完成遗留问题。当前公网部署与询盘已通过，优先补齐线上备份恢复；不覆盖当前站点、不向外发邮件。

## 实施

使用官方 CLI 创建专用空数据库 rec0920；恢复目录位于主机 HOME 的 .harness-recovery 下，备份位于 .harness-backups 下，均不在 document root。再次导出当前 B2B 站，包括已有询盘，并在导出前后对 11 张业务表做行排序哈希对比，避免变动中的快照被误判。恢复后逐文件哈希、业务表哈希、插件加载验收；克隆配置禁用 cron/自动更新、URL 指向 restore.invalid，平台 MU 在隔离副本不运行。

## 问题与处理

首次 create 缺少 website-domain，CLI 在本地快速失败且未保留具体错误正文；先查数据库列表确认目标未出现，再补齐网站参数调用，返回 accepted 后再次查询实际列表，确认独立库已创建。不能将 accepted 当作完成，也不能在未知结果时重放。

保护目录 HTTP 403 再次实测后才上传临时数据库凭据；凭据不进入公开输出。恢复任务原子锁防重放。

## 当前状态

恢复任务已准备并提交调度，结果待验证。正式邮件、线上后台修改回显、商业内容/视觉仍未完成。

## 实测结果

恢复成功：当前线上快照恢复到独立数据库/非公开目录，11 张业务表哈希一致，8879 文件哈希一致，19 产品、2 询盘保留，主题/ACF/Fluent Forms 在副本正常加载。临时执行文件删除，cron 已删除。该验收是同主机隔离 CLI 恢复，不是原位回滚、跨机灾备或副本浏览器验收。副本和备份保留用于复核。公开证据 recovery.json。

用户追加要求：继续覆盖 CMS 后台所有页面编辑、图片上传与替换等完整功能，另立后续 CMS 验收阶段，保留当前恢复证据版本。
