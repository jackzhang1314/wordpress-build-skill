# 0924-05 全新空站部署与询盘验证

- 时间：2026-09-24 20:15 Asia/Shanghai
- 触发原因：完成 Starter Template 从零建站全流程验收。
- 验证对象：
  - 本地项目：`/Users/Zhuanz1/Documents/ChatGPT/wordpress-projects/starter-component-verify`
  - 线上站点：https://aqua-termite-784698.hostingersite.com
  - 分支：`codex/component-starter-rebuild`
- 流程结果：
  - Hostinger provision：生成 `aqua-termite-784698.hostingersite.com`，创建网站与 WordPress 成功。
  - 首次 deploy：发现致命错误并自动回滚，证明 rollback 保护有效。
  - 根因：`components.php` 在 `Starter\Theme` 命名空间内使用 `new WP_Query()`，缺少全局反斜杠。
  - 修复后重新部署：23/23 页面 200，每页 H1=1、0 跳级；内容计数 Products 6 / Categories 4 / Industries 3 / Guides 3 / News 2。
  - `verify --screenshots`：69/69 截图成功（23 路由 × 390/768/1440）。
- 表单新发现与修复：
  - 全新 Fluent Forms 表单不能只写基础 fields；新版还要求 `submitButton`。
  - `formSettings` 采用可渲染的简化结构，而不是后台导出的完整结构。
  - 自动创建表单改为复制 Fluent Forms 自带 Demo 的可渲染结构，再替换标题、字段和通知。
  - 前台 Contact 页表单实际渲染成功。
  - 浏览器实际提交成功，页面出现 “Thank you for your message...”。
  - 数据库校验：Form ID 8，Submissions 1，最新 submission status `unread`。
- 关键提交：
  - `2939560 fix(starter): qualify WordPress classes and add namespace gate`
  - 最新 plugin/测试提交见 `git log -3`
- 验证命令：
  - `npm run typecheck`
  - `npm run lint`
  - `npm test`
  - `node harness/cli.mjs --project examples/classic-b2b-starter check`
  - `node harness/cli.mjs --project /path/to/starter-component-verify deploy --skip-content`
  - `node harness/cli.mjs --project /path/to/starter-component-verify verify --screenshots`
- 遗留：
  - SMTP 当前依赖 wp-config 常量；此验证站未配置企业邮箱，因此只验证入库，不验证外发。
  - GitHub Template Repo 与 release 包发布待做。
