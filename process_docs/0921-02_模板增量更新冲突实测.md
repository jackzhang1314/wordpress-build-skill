# 模板增量更新冲突实测

时间：2026-09-21T01:16:48.965710+08:00（Asia/Shanghai）。
触发：继续验证架构未覆盖的增量更新和后台模板冲突。

实现 scripts/template-update-check.mjs（位于 examples/b2b-block-starter），加入 npm run test:starter:template-update。读取现有实验环境指针，限定 reuse loopback URL 与 NEW_SITE_REFERENCE_LAB；唯一测试名避免覆盖现有对象，finally 清理本轮产品/模板/文件。未操作线上站点。

结果：数据库覆盖优先；文件第二版存在但不取代后台模板；产品正文和 ACF 保持；已有询盘三表哈希不变；只删除测试覆盖后解析到第二版文件；清理通过。证据 docs/acceptance/template-update/report.json。

沉淀：release.md 增加覆盖检测、保留/合并/重置决策及禁止自动清空后台模板；ARCHITECTURE 更新专项状态。本次无执行失败。

边界：只验证模板解析与文件更新，不是匿名浏览器页面验收或全量代码部署；未验证模板部件/全局样式、部署期间新增询盘、媒体更新、插件数据库迁移及远端传输。下一阶段应把覆盖清单接入增量发布预检，再在独立完整安装执行增量包和故障恢复。

## 同阶段追加：只读覆盖预检

时间：2026-09-21T01:25:18.757193+08:00。新增 release/overrides.mjs 只读扫描与 update-preflight.mjs 本地入口；参数限制主题相对模板路径，拒绝越界、非模板文件和空命令输入；冲突返回 2，不自动重置。公开结果不含正文。

真实 WordPress 模板冲突/无关路径/无写入/重置后无冲突回归通过；路径边界 node test 通过，npm run lint 通过，git diff --check 通过。测试夹具已清理，现有实验站扫描无覆盖。当前报告为 local-reuse-template-file-update 范围，未升级为完整部署验收。

剩余：模板部件和全局样式专项夹具、Hostinger 读取适配、自动从实际增量包生成变更路径、冲突选择与备份/回退，以及部署中新增询盘保留。不能把手动输入路径的本地预检称为完整自动门禁。

## 同阶段追加：自动差异和更多覆盖类型

时间：2026-09-21T01:36:26.829628+08:00。增加 theme-diff.mjs 与 --compare 模式，比较完整主题目录 SHA-256，包含文件删除，非模板变化明确需要页面回归，缺主题标识/符号链接拒绝。两个 node tests 和 lint 通过。实际 WordPress 验证模板部件/全局样式冲突、他主题排除、内容不变；修正夹具曾残留空 wp_theme 测试 term 的清理遗漏，已删除本轮明确归属空 term，并改用唯一 term + finally 清理后复跑。

边界：主题目录对比不是远端基线获取或完整部署包认证；测试覆盖扫描而非全局样式视觉合并。无冲突和零差异均不自动批准发布。

## 同阶段追加：Hostinger 只读基线

时间：2026-09-21T02:42:58.585665+08:00。新增 scripts/hostinger/theme-baseline.mjs，通过官方 CLI 分页列目录并并行读取最多 4 个文本文件，对比本地候选主题，保留响应内容 SHA-256 和未验项。未写入线上。REST 读取 17 模板/2 部件，当前自定义覆盖为 0；全局样式数据库覆盖不在该次 REST 报告范围。

实际失败：max-depth 30 被接口拒绝（最大 10）；改为 10 并拒绝到达边界的目录。OFL-body.txt 内容比 size_bytes 少一字节，检查为末尾 LF 被接口去除；新增严格限定的归一化匹配分类，不冒充远端原始文件哈希。未知差异仍停止。二进制字体不能通过该文本接口读取，保留未验。

验证：线上结果保存 docs/acceptance/template-update/hostinger-baseline.json 与 hostinger-overrides.json；lint 通过。下一步是二进制/插件及数据库覆盖完整基线与增量传输，当前只读比对不替代完整更新验收。
