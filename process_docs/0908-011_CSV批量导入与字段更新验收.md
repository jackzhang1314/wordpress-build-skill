# CSV 批量导入与字段更新验收

- 日期与时间：2026-09-08 11:55:12 GMT+8。
- 触发原因：批量导入模块完成并通过关键测试。
- 主要工作：新增 import-plan/import-apply，CSV 字符串值按显式列映射进入真实字段；源文件哈希、稳定业务键、全批预验证、逐记录持久化回执，最多 20 条/1 MiB。重复执行不重新创建，未知写结果沿用原日志阻断规则。
- 真实验证：原有产品 22 发布后仅修改 acf.oct_material，正文哈希不变；匿名 1440/390px 页面 HTTP200、显示新值、单一 H1、无横向溢出。两条 CSV 产品创建为草稿 24/25；第二次执行返回相同 ID，总产品数仅从 1 增至 3。
- 测试：20 项测试通过；新增源文件变化拒绝写入、第二条回读失败后恢复不重复 POST、引号/换行/前导零及重复列/键校验。typecheck/lint 通过。
- 关键文件：src/import-content.ts、tests/import-content.test.ts、src/content-model.ts、src/content.ts、src/cli.ts、examples/products.csv、examples/product-import.json；证据在 .lab/artifacts/import-evidence.json、cms-field-evidence.json 与 output/playwright/cms-after-result.txt。
- 兼容修正：WordPress supports.editor 可为配置参数数组；ACF 空字段 schema 的 properties 可能返回空数组。读取本地官方源码及实际 REST 后兼容，非空数组不冒充字段映射。
- 下一步：目录页原生区块及公开列表验证、产品模板适配、表单/SEO/其他素材及恢复接口。产品页面默认文章模板问题仍未解决。
