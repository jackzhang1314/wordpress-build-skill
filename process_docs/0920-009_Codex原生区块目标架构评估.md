# Codex 原生区块目标架构评估

时间：2026-09-20 05:18 Asia/Shanghai（UTC+08:00）。

触发：用户再次强调要优秀的 WordPress AI 建站方案、不使用 Elementor/第三方编辑器，由 Codex 生成页面与样式，结合此前的后台模板复用要求重新评估。

工作：读取官方 wp-block-themes/wp-patterns Skill 及相关模板参考，核对 WordPress 官方 customTemplates、CSS、Patterns、Bindings、动态块、锁定与模板覆盖文档。新增 docs/TARGET-ARCHITECTURE.md，明确推荐区块主题作为待验证目标主线；PHP 混合路线仍是当前运行实现，不误称已迁移。

结论：Codex 可生成原生区块模板与定制样式，业务插件/ACF 数据和现有验证资产继续复用。分类布局、字段绑定、数据库模板覆盖及编辑权限须单独适配。区块主题不自动保证设计优秀，PHP 也不是因版本落后而弃用；推荐依据为原生模板与维护目标的契合。

发现自有 design reference 的 Pattern class 规范与上游 markup-only 约束有差异，目标文档要求实施前明确模块职责，不改 vendor。本轮没有更改默认 Skill、主题代码、模型或数据，也未将推荐方向写成行为验收通过。

验证：核对官方一手来源与文档链接；代码未修改，不运行无关功能测试。后续门槛为独立主题中首页、两产品模板、两分类布局的前后台/视觉/业务/恢复对照验收，再决定并统一更新默认流程。
