# Environment

Use `node wordpress-builder.mjs bootstrap --fix` for dependency and runtime repair.

Required: Node 22+, npm, Git, rsync, tar, gzip, project dependencies, Skill runtime and Hostinger CLI.

Recommended: PHP or Docker for PHP syntax checks, Chrome for browser verification.

After repair, run `doctor` and verify SSH/WP-CLI when a project is configured.
