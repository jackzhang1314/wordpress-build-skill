# 客户网站：Codex 项目约定

此文件是生成模板，尚未启用。实施人员应查证并替换下列待核对项，将完成稿保存到客户源码根目录 AGENTS.md；路径相对该根目录。未确定项保留明确状态，不填虚构路径或版本。

## 项目事实与入口

- 网站/行业/目标市场/语言：待核对。
- 范围：新建 B2B 网站及其后续维护，不使用 Elementor 或其他第三方页面编辑器。
- Brief、站点地图、架构、内容模型、设计 token、验收记录的实际路径：待核对；复用现有文档，不重复维护同一事实。
- 主题目录、业务插件目录、构建目录：待核对。
- 开发/预览/生产 URL、启动方式、实测 WordPress/PHP/数据库/ACF 版本与核对时间：待核对。只记录凭据来源的名称或位置，不写密钥。
- wordpress-builder 的实际安装路径及 SKILL.md：待核对。开始建站工作先读该入口，按任务使用官方专业模块；只读取所需 references。

## 架构与内容约束

采用原生区块主题、原生可选模板/Patterns、业务插件 CPT/分类/ACF，以及必要 PHP 动态模块。模板与样式归主题，业务数据归插件和数据库；同一产品换模板不复制数据。特殊整页 PHP 例外记录理由、路由、编辑范围及验收。

新页面按 Skill 的 search-quality.md 明确买家需求、页面独立价值及事实来源，按 seo.md 实施服务端正文、真实内链、索引策略和结构化数据。产品事实仅存一份；不得编造认证、客户或评价。演示内容明确标识，正式发布前核对。

SEO 输出责任插件/代码、询盘组件、产品与分类模板清单、字段与编辑权限的真源位置：待核对。编辑模板前检查是否有数据库模板覆盖，避免源码与前台不一致。

## 执行与验证

- 开工检查 Git 状态、站点身份与当前环境，保留已有修改。写内容按 plan → apply → readback；未知结果先查日志和远端状态，避免重复写入。
- 实际安装/启动/构建/lint/类型检查/测试命令及工作目录：待核对。未配置检查明确记录缺口，不照搬工具仓库命令。
- 页面或模板变更核对真实前台、移动端、后台字段/图片/正文回显；整站交付增加询盘、SEO、部署及恢复验收。性能、视觉、邮件送达、恢复分别记录证据与未覆盖项。
- 公网供应商：Hostinger Managed WordPress；实际套餐、站点标识、SSH 别名、路径、staging/production URL 和部署命令：待核对。遵循 Skill 的 references/hostinger.md；私有配置不提交，MCP 可选，首次发布与后续更新分开，禁止用本地库覆盖线上询盘。
- 发布目标与已有授权范围：待核对。文档本身不构成发布、发送邮件或删除数据授权；已有用户授权持续有效，不重复索要例行确认。
- 验收记录保存日期、实际环境/版本、源码状态和证据。项目进度放现有状态文档；本文件只维护稳定约束和入口。

## 经验回写

本项目执行中所有实际失败、阻塞、错误判断和用户纠正都在阶段收尾前记录触发条件、版本、原因、处理及回归证据，同类合并；未验证问题也须留下待办。验证有效的做法再提炼复用。项目特例留项目文档；稳定约束更新本 AGENTS；通用方法回写实际使用的 Skill/reference/playbook，能自动防止复发的环节补脚本或测试。未经验证只列待验证，旧结论失效时更新真源并保留历史依据；交付说明沉淀位置，避免只在聊天中保留经验。
