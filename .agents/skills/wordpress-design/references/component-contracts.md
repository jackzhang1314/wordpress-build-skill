# Component Contracts

Every component must define:

1. purpose and user action;
2. template/part owner;
3. CSS class contract;
4. data source and editability;
5. empty, populated and mobile states;
6. heading hierarchy;
7. focus/keyboard behavior;
8. verification evidence.

Do not duplicate a semantic component in a page template. Move repeated markup to a component, include or pattern.
